apiVersion: v1
kind: Service
metadata:
  name: api-gateway
  namespace: kube-lifecycle
spec:
  selector:
    app: api-gateway
    color: blue
  ports:
  - protocol: TCP
    port: 80
    targetPort: 80
  type: LoadBalancer # Internet facing entry point
